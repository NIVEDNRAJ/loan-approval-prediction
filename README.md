# 🏦 Loan Approval Predictor

A machine learning web app built with **Python**, **Streamlit**, and **Scikit-learn** to predict loan approval chances.

## 🚀 How to Run Locally
1. Install dependencies  
   ```bash
   pip install -r requirements.txt
Run the app

streamlit run app.py
